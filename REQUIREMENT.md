# 全栈测试题：实现一个简单电商系统

请使用任意技术栈实现一个可本地运行的电商 MVP，覆盖前端、后端 API、数据库的完整链路。

---

## 基本要求

实现一个支持以下**完整用户流程**的电商系统：

1. 用户注册 / 登录
2. 浏览商品列表（支持分页）
3. 查看商品详情
4. 将商品加入购物车 / 修改数量 / 移除
5. 提交订单（库存须真实扣减）
6. 查看我的订单列表及订单详情

以上六步必须全部可操作，这是最低验收线。

---

## 技术要求

### 前端

- 使用任意前端框架（React、Vue、Svelte、原生 JS 等均可）
- 必须有独立的前端页面，不接受纯 API 交付
- 页面需基本可用，布局不要求精美，但操作流程要清晰

### 后端

- 使用任意语言和框架（Node.js、Python、Go、Java 等均可）
- 必须提供 HTTP API（RESTful 或 GraphQL 均可）
- 前后端必须分离，通过 API 通信

### 数据库

- 必须使用关系型数据库（PostgreSQL、MySQL 或 SQLite 均可）
- 需提供建表脚本或 migration 文件
- 不允许将数据存储在内存或文件 JSON 中

### 运行方式

- 提供 `docker compose up` 一键启动，**或**提供详细的本地启动说明
- README 必须说明如何初始化数据库、如何启动前后端

---

## 功能细节

### 用户与认证

- 用户注册时至少需要：用户名、邮箱、密码
- 密码必须加密存储（bcrypt 或等效方案），**明文存储直接不合格**
- 登录后使用 JWT 维持会话
- 受保护的接口（购物车、下单、订单）须验证身份

### 商品

- 商品至少包含：名称、描述、价格、库存数量、图片（URL 字符串即可）
- 数据库中预置至少 10 件商品（通过 seed 脚本或 migration）
- 商品列表支持分页，每页至少显示 6 件商品

### 购物车

- 购物车与用户绑定，刷新后不丢失（存数据库，不存 localStorage）
- 同一商品加入多次应合并数量，而非重复添加
- 购物车中显示每件商品的小计和总金额

### 订单

- 提交订单时须在同一事务内完成：创建订单记录 + 扣减库存
- 如库存不足，须返回明确错误，不能静默失败
- 订单状态至少包含：待处理（pending）
- 订单记录须保存下单时的商品价格（不随商品价格更新而变化）

---

## API 设计要求

- 接口须遵循统一规范（RESTful 路径命名，或 GraphQL schema）
- 错误响应须包含有意义的错误信息和合适的 HTTP 状态码（400、401、404、409、500 等）
- 接口须有简要文档（README 中列出，或提供 OpenAPI/Swagger，或 Postman collection）

---

## 加分项

以下功能不是必须，但可以加分：

1. **商品搜索**（按名称关键词搜索）
2. **商品分类**（商品属于某个分类，支持按分类筛选）
3. **订单取消**（允许用户取消 pending 状态的订单，并回滚库存）
4. **库存并发保护**（通过数据库事务 + 行锁或乐观锁防止超卖）
5. **Refresh Token**（登录态自动续期，而非 JWT 过期后直接退出）
6. **单元测试**（后端核心逻辑，如订单创建、库存扣减）
7. **TypeScript**（前端或后端任意一端使用）
8. **API 文档**（OpenAPI/Swagger 或 Postman collection）
9. **响应式前端**（移动端可用）
10. **Docker Compose 一键启动**（无需手动配置数据库）

---

## 交付要求

请提交：

1. 源代码（前端 + 后端）
2. 数据库建表脚本 / migration 文件
3. 包含以下内容的 README：
   - 使用的技术栈
   - 本地启动步骤
   - API 接口列表（路径、方法、简要说明）
   - 数据库表结构说明
   - 核心业务逻辑说明（订单创建流程、库存扣减方案）
4. 如有 AI 辅助，请说明使用方式

---

## 验收标准

重点检查：

1. **六步用户流程是否全部可操作**（最低验收线）
2. **密码是否加密存储**（明文存储一票否决）
3. **库存扣减是否在事务内完成**（超卖是严重 bug）
4. **购物车是否持久化至数据库**
5. **订单是否保存下单时的历史价格**
6. **API 错误处理是否规范**
7. **代码结构是否清晰，各层职责是否分明**

---

## 评分维度

| 维度 | 权重 | 说明 |
|------|:----:|------|
| 功能完整性（六步流程） | 30% | 每个步骤可用得满分，不可用扣分 |
| 数据库设计 | 20% | 表结构合理、关系正确、有必要的索引 |
| API 设计与错误处理 | 20% | 规范、完整、状态码正确 |
| 后端安全与正确性 | 20% | 密码哈希、JWT、事务、并发 |
| 代码质量与文档 | 10% | 结构清晰、README 完整 |
